@model AppWeb.Models.Login

@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Iniciar Sesión - GameStore</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    <style>
        body {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .login-box {
            width: 100%;
            max-width: 450px;
            padding: 40px;
        }

        .form-control {
            background: rgba(0, 0, 0, 0.4) !important;
            border: 1px solid var(--steam-blue-medium) !important;
            color: var(--steam-text-bright) !important;
            border-radius: 2px !important;
        }

        .form-control:focus {
            border-color: var(--steam-blue-light) !important;
            box-shadow: 0 0 8px rgba(102, 192, 244, 0.3) !important;
        }

        label {
            color: var(--steam-blue-light);
            text-transform: uppercase;
            font-size: 0.8rem;
            letter-spacing: 1px;
            font-weight: 600;
        }
    </style>
</head>
<body>

    <div class="login-box glass-panel shadow-lg animate__animated animate__fadeIn">

        <div class="text-center mb-4">
            <h2 class="fw-bold"><i class="fa-solid fa-gamepad me-2 text-info"></i>GAMESTORE</h2>
            <p class="text-muted small">CONECTA CON TU BIBLIOTECA</p>
        </div>

        @if (ViewBag.Error != null)
        {
            <div class="alert alert-danger bg-danger text-white border-0 py-2 small mb-4">
                <i class="fas fa-exclamation-circle me-2"></i> @ViewBag.Error
            </div>
        }
        @if (TempData["SuccessMessage"] != null)
        {
            <div class="alert alert-success bg-success text-white border-0 py-2 small mb-4">
                <i class="fas fa-check-circle me-2"></i> @TempData["SuccessMessage"]
            </div>
        }

        <form asp-action="Login" method="post">
            @Html.AntiForgeryToken()
            <div class="mb-4">
                <label class="form-label">Nombre de cuenta</label>
                <input asp-for="correo" class="form-control" autocomplete="off" />
                <span asp-validation-for="correo" class="text-danger small"></span>
            </div>
            <div class="mb-4">
                <label class="form-label">Contraseña</label>
                <input asp-for="Contrasena" type="password" class="form-control" />
                <span asp-validation-for="Contrasena" class="text-danger small"></span>
            </div>

            <button type="submit" class="btn btn-accent w-100 py-2 mb-3">
                INICIAR SESIÓN
            </button>

            <div class="text-center mt-4">
                <p class="small text-muted mb-1">¿Aún no eres parte de la comunidad?</p>
                <a href="@Url.Action("Registro", "Account")" class="text-info text-decoration-none fw-bold">CREAR UNA CUENTA GRATIS</a>
            </div>
        </form>

    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    @await Html.PartialAsync("_ValidationScriptsPartial")
</body>
</html>

@* @model AppWeb.Models.Login

@{
    Layout = null;
}

<div class="container mt-5">
    <div class="col-md-4 mx-auto">
        <div class="card p-4 shadow rounded-4">
            <h3 class="text-center mb-3">Iniciar Sesion</h3>
            @if (ViewBag.Error != null)
            {
                <div class="alert alert-danger">
                    @ViewBag.Error
                </div>
            }
            <form method="post" asp-action="login">
                <div class="mb-3">
                    <label>Correo</label>
                    <input type="email" class="form-control" asp-for="correo" required />
                </div>
                <div class="mb-3">
                    <label>Contraseña</label>
                    <input type="password" class="form-control" asp-for="Contrasena" required />
                </div>
                <button class="btn btn-accent w-100">
                    Ingresar
                </button>
            </form>
        </div>
    </div>
</div>
 *@